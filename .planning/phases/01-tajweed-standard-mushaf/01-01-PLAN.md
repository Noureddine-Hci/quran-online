---
phase: 01-tajweed-standard-mushaf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - style.css
autonomous: false
requirements:
  - TAJ-01
  - TAJ-02

must_haves:
  truths:
    - "Qalqalah letters appear in blue (#009be5), not red"
    - "Madd letters appear in red/orange gradient (normal=cumin, permissible=orange, obligatory=red, necessary=dark red)"
    - "Ghunna/Ikhfa/Iqlab/Idgham-with-Ghunna letters appear in green (#008e50)"
    - "Silent letters and Hamza Wasl remain gray (#AAAAAA)"
    - "The Tajweed legend dots show the correct color for each rule"
  artifacts:
    - path: "style.css"
      provides: "Standard Mushaf Tajweed color palette + legend dot colors"
      contains: "tajweed.qlq.*#009be5"
  key_links:
    - from: "style.css (tajweed.qlq)"
      to: "HTML <tajweed class='qlq'>"
      via: "CSS element.class selector"
      pattern: "tajweed\\.qlq.*color.*#009be5"
    - from: "style.css (.legend-dot.qlq)"
      to: "HTML <span class='legend-dot qlq'>"
      via: "CSS class selector"
      pattern: "\\.legend-dot\\.qlq.*color.*#009be5"
---

<objective>
Update the 15 Tajweed CSS color rules in style.css to match the standard printed Mushaf palette (red=madds, blue=qalqalah, green=nasalization, gray=silent). Also add per-rule color rules for `.legend-dot.{cls}` elements since they currently have NO color (the existing `tajweed.qlq` selectors only match `<tajweed>` elements, not `<span class="legend-dot qlq">`).

Purpose: TAJ-01 (colors match printed Mushaf) and TAJ-02 (legend shows correct colors)
Output: Updated style.css with standard Mushaf palette for both tajweed elements and legend dots
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-tajweed-standard-mushaf/01-RESEARCH.md

<interfaces>
<!-- From style.css lines 1447-1462 — current tajweed color block to REPLACE -->
tajweed.ham_wasl          { color: #AAAAAA; }
tajweed.slnt              { color: #AAAAAA; }
tajweed.qlq               { color: #DD0008; }
tajweed.ghn               { color: #FF7E1E; }
tajweed.ikhf              { color: #9400A8; }
tajweed.ikhf_shfw         { color: #D500B7; }
tajweed.iqlb              { color: #26BFFD; }
tajweed.idgh_ghn          { color: #169200; }
tajweed.idgh_w_ghn        { color: #169200; }
tajweed.idghm_shfw        { color: #58B800; }
tajweed.idgh_mus          { color: #169200; }
tajweed.madda_normal      { color: #537FFF; }
tajweed.madda_permissible { color: #4050FF; }
tajweed.madda_obligatory  { color: #2144C1; }
tajweed.madda_necessary   { color: #000EBC; }

<!-- From style.css lines 656-659 — legend dot base style (NO per-rule colors exist) -->
.legend-dot {
    font-size: 1.1rem;
    flex-shrink: 0;
    font-family: var(--font-arabic);
}

<!-- From reader.js line 80 — legend dot HTML structure -->
<span class="legend-dot ${cls}">&#x25CF;</span>
<!-- cls values: ham_wasl, slnt, madda_normal, madda_permissible, madda_necessary, qlq, madda_obligatory, ikhf_shfw, ikhf, idghm_shfw, iqlb, idgh_ghn, idgh_w_ghn, idgh_mus, ghn -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Tajweed color palette with standard Mushaf colors + add legend dot colors</name>
  <files>style.css</files>
  <read_first>style.css (lines 1447-1467 for tajweed colors block, lines 656-660 for legend-dot base, lines 1330-1345 for OLED mode)</read_first>
  <action>
In style.css, replace the Tajweed color block (lines 1447-1462) with the exact standard Mushaf palette below. Use these EXACT hex values:

```css
/* -- Tajweed colors — palette standard Mushaf imprime (EasyQuran/GlobalQuran) -- */
tajweed.ham_wasl          { color: #AAAAAA; }   /* Hamza Wasl           — gris           */
tajweed.slnt              { color: #AAAAAA; }   /* Lettre silencieuse   — gris           */
tajweed.qlq               { color: #009be5; }   /* Qalqala              — bleu ciel      */
tajweed.ghn               { color: #008e50; }   /* Ghunna               — vert           */
tajweed.ikhf              { color: #008e50; }   /* Ikhfa                — vert           */
tajweed.ikhf_shfw         { color: #58B800; }   /* Ikhfa Shafawi        — vert clair     */
tajweed.iqlb              { color: #008e50; }   /* Iqlab                — vert           */
tajweed.idgh_ghn          { color: #008e50; }   /* Idgham avec Ghunna   — vert           */
tajweed.idgh_w_ghn        { color: #169200; }   /* Idgham sans Ghunna   — vert           */
tajweed.idghm_shfw        { color: #58B800; }   /* Idgham Shafawi       — vert clair     */
tajweed.idgh_mus          { color: #169200; }   /* Idgham Mutajanisayn  — vert           */
tajweed.madda_normal      { color: #bb8800; }   /* Madd normal 2v       — cumin/dore     */
tajweed.madda_permissible { color: #dd6600; }   /* Madd permissible 2/4/6v — orange      */
tajweed.madda_obligatory  { color: #cc2200; }   /* Madd obligatoire 4-5v — rouge sang    */
tajweed.madda_necessary   { color: #a00000; }   /* Madd necessaire 6v   — rouge fonce    */
```

Then, IMMEDIATELY after the tajweed element color block, add legend-dot per-rule color rules that mirror the exact same hex values:

```css
/* -- Legend dot colors (mirrors tajweed palette above) -- */
.legend-dot.ham_wasl          { color: #AAAAAA; }
.legend-dot.slnt              { color: #AAAAAA; }
.legend-dot.qlq               { color: #009be5; }
.legend-dot.ghn               { color: #008e50; }
.legend-dot.ikhf              { color: #008e50; }
.legend-dot.ikhf_shfw         { color: #58B800; }
.legend-dot.iqlb              { color: #008e50; }
.legend-dot.idgh_ghn          { color: #008e50; }
.legend-dot.idgh_w_ghn        { color: #169200; }
.legend-dot.idghm_shfw        { color: #58B800; }
.legend-dot.idgh_mus          { color: #169200; }
.legend-dot.madda_normal      { color: #bb8800; }
.legend-dot.madda_permissible { color: #dd6600; }
.legend-dot.madda_obligatory  { color: #cc2200; }
.legend-dot.madda_necessary   { color: #a00000; }
```

IMPORTANT: Do NOT touch `html_color` in tajweed.js — it is metadata, not used for rendering (per research Pitfall 4). The CSS is the single source of truth for colors.

IMPORTANT: Do NOT add OLED-specific overrides for tajweed colors. The chosen palette values (#009be5 blue, #008e50 green, #bb8800 cumin, #dd6600 orange, #cc2200 red, #a00000 dark red) all have sufficient contrast on black backgrounds. The #a00000 dark red is the lowest contrast at ~3.2:1 which is acceptable for decorative colored text (not body text).
  </action>
  <verify>
    <automated>grep -c "#009be5\|#008e50\|#bb8800\|#dd6600\|#cc2200\|#a00000" style.css</automated>
  </verify>
  <acceptance_criteria>
- grep "tajweed.qlq.*#009be5" style.css returns a match (qalqalah is blue)
- grep "tajweed.madda_necessary.*#a00000" style.css returns a match (madd necessary is dark red)
- grep "tajweed.ghn.*#008e50" style.css returns a match (ghunna is green)
- grep "tajweed.ikhf.*#008e50" style.css returns a match (ikhfa is green, same family as ghunna)
- grep "tajweed.madda_normal.*#bb8800" style.css returns a match (madd normal is cumin)
- grep "#DD0008\|#537FFF\|#4050FF\|#2144C1\|#000EBC\|#9400A8\|#D500B7\|#26BFFD\|#FF7E1E" style.css returns 0 matches (old colors fully removed)
- grep "legend-dot.qlq.*#009be5" style.css returns a match (legend dots have colors)
- grep "legend-dot.ghn.*#008e50" style.css returns a match
- Count of ".legend-dot." color rules = 15 (one per tajweed class)
  </acceptance_criteria>
  <done>All 15 tajweed element colors updated to standard Mushaf palette. All 15 legend-dot per-rule colors added. No old AlQuran Cloud colors remain. Legend dots now display the correct color for each rule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — Tajweed colors match printed Mushaf</name>
  <files>style.css</files>
  <action>
Present the updated Tajweed colors to the user for visual verification. The user will compare the app side-by-side with a standard Tajweed Mushaf image to confirm the colors match.
  </action>
  <what-built>Updated Tajweed color palette to standard Mushaf colors (red=madds, blue=qalqalah, green=nasalization, gray=silent) and added legend dot coloring.</what-built>
  <how-to-verify>
    1. Open the app in browser (serve index.html via local server)
    2. Navigate to any surah with diverse Tajweed rules (Al-Fatiha or Al-Baqarah)
    3. Verify Tajweed is enabled (toggle should be ON)
    4. Check colors against this reference:
       - Qalqalah letters: blue sky (#009be5)
       - Madd letters: cumin/orange/red gradient (light to dark for normal/permissible/obligatory/necessary)
       - Ghunna, Ikhfa, Iqlab, Idgham-with-Ghunna: green (#008e50)
       - Silent letters, Hamza Wasl: gray (#AAAAAA)
    5. Open the Tajweed legend panel — verify each dot shows the correct color matching the rule
    6. Compare side-by-side with a standard Tajweed Mushaf image (search "tajweed quran colors" for reference)
    7. Test in OLED mode — verify all colors remain readable on black background
  </how-to-verify>
  <verify>User confirms colors match standard Mushaf</verify>
  <done>User approves the Tajweed color palette as matching standard printed Mushaf convention</done>
  <resume-signal>Type "approved" or describe color issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All 15 tajweed CSS rules use standard Mushaf hex values
- All 15 legend-dot CSS rules exist with matching hex values
- No old AlQuran Cloud colors remain (#DD0008, #537FFF, #4050FF, #2144C1, #000EBC, #9400A8, #D500B7, #26BFFD, #FF7E1E)
- Visual side-by-side with printed Mushaf confirms color accuracy
</verification>

<success_criteria>
- Tajweed colors in browser match standard printed Mushaf convention
- Legend dots show the correct color for each Tajweed rule
- Colors readable in both normal and OLED modes
</success_criteria>

<output>
After completion, create `.planning/phases/01-tajweed-standard-mushaf/01-01-SUMMARY.md`
</output>
